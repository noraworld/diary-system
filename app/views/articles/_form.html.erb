<%= form_for @article do |f| %>
  <% if flash[:alert] %>
  <div id="failed-message">
    <%= flash[:alert] %>
  </div>
  <% end %>

  <div id="textarea">
    <%= f.text_area :text, {:id => 'post-content'} %>
    <%= f.submit({:id => 'post-button'}) %>
  </div>

<% end %>
